{
  "name": "barreliq-mobile",
  "version": "0.7.0",
  "private": true,
  "description": "Capacitor native shell (Android/iOS) for the coach/ web app. Does not duplicate coach/ - webDir points at ../coach directly.",
  "scripts": {
    "sync:android": "cap sync android",
    "sync:ios": "cap sync ios",
    "open:android": "cap open android",
    "open:ios": "cap open ios",
    "run:android": "cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/core": "6.2.1",
    "@capacitor/ios": "^6.2.1"
  },
  "devDependencies": {
    "@capacitor/cli": "6.2.1"
  }
}
